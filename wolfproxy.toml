[server]
host = "0.0.0.0"
http_port = 80
https_port = 443

[nginx]
# Path to nginx configuration directory
# WolfProxy will read from sites-enabled and conf.d
config_dir = "/home/paulc/NetBeansProjects/wolfproxy"

# Automatically reload configuration when files change
auto_reload = false

[monitoring]
# Enable the monitoring web interface
enabled = true
# Port for the monitoring server
port = 5001
# HTTP Basic Auth credentials
username = "admin"
password = "admin"
